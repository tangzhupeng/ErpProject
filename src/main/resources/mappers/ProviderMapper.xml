<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.gson.jindie.model.mapper.salemapper.ProviderMapper">
    <select id="findAllProvider"  resultType="cn.gson.jindie.model.pojos.txypojos.ErpProvider">
        select *  from erp_provider
    </select>

</mapper>